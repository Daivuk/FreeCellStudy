#pragma once

#include <onut/Color.h>
#include <onut/Point.h>

static const int PIXEL_SIZE = 4;
static const Point PIXEL_RESOLUTION = {256, 192};
static const Color CLEAR_COLOR = OColorHex(323c39);
static const Color TEXT_COLOR = OColorHex(9badb7);
static const Color ACTIVE_TEXT_COLOR = OColorHex(cbdbfc);
static const float TOP_OFFSET = 5.f;
